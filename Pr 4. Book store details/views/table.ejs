<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Users</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 20px;
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            min-height: 100vh;
        }

        h1 {
            text-align: center;
            background: rgba(0, 0, 0, 0.5);
            padding: 12px 24px;
            border-radius: 10px;
            display: inline-block;
            font-size: 26px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .table-container {
            width: 95%;
            max-width: 1000px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            color: white;
            font-size: 16px;
            text-align: left;
        }

        th, td {
            padding: 14px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
            text-align: center;
        }

        th {
            background: rgba(0, 0, 0, 0.4);
            font-weight: bold;
        }

        tbody tr {
            transition: background 0.3s ease-in-out;
        }

        tbody tr:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        .btn {
            padding: 8px 14px;
            border-radius: 8px;
            font-size: 14px;
            font-weight: bold;
            color: white;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-block;
            text-align: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
        }

        .btn-delete {
            background: linear-gradient(90deg, #ff4b1f, #ff9068);
        }

        .btn-edit {
            background: linear-gradient(90deg, #36d1dc, #5b86e5);
        }

        .btn:hover {
            transform: translateY(-2px);
        }

        .form-link {
            display: inline-block;
            margin-top: 25px;
            padding: 12px 24px;
            background: linear-gradient(90deg, #2c3e50, #4ca1af);
            color: white;
            border-radius: 10px;
            font-size: 16px;
            font-weight: bold;
            text-decoration: none;
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
        }

        .form-link:hover {
            transform: scale(1.05);
        }
    </style>
</head>

<body>

    <h1>View Users</h1>

    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Book Name</th>
                    <th>Description</th>
                    <th>Price</th>
                    <th>Author</th>
                    <th>Pages</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% allrecord.map((val, i) => { %>
                    <tr>
                        <td><%= val.id %></td>
                        <td><%= val.userbookname %></td>
                        <td><%= val.userdescripion %></td>
                        <td>$<%= val.userprice %></td>
                        <td><%= val.userauthorname %></td>
                        <td><%= val.userpage %></td>
                        <td>
                            <a href="/deleteid?did=<%= val.id %>" class="btn btn-delete">🗑️ Delete</a>
                            <a href="/edituser?eid=<%= val.id %>" class="btn btn-edit">✏️ Edit</a>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <a href="/" class="form-link">➕ Add New Book</a>

</body>

</html>
